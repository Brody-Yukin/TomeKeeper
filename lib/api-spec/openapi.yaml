openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: books
    description: Book identification operations
paths:
  /books/identify-cover:
    post:
      operationId: identifyBookCover
      tags: [books]
      summary: Identify a book from a cover photo
      description: Accepts a base64-encoded cover photo and returns structured cover analysis extracted with server-side image recognition.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifyCoverRequest"
      responses:
        "200":
          description: Cover analysis result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CoverAnalysis"
        "400":
          description: Invalid request (bad MIME type or image too large)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /books/isbn/{isbn}:
    get:
      operationId: getBookByIsbn
      tags: [books]
      summary: Look up a book by ISBN
      description: Validates and normalizes an ISBN-10 or ISBN-13, then queries Google Books with a server-only key and Open Library as a fallback.
      parameters:
        - name: isbn
          in: path
          required: true
          schema:
            type: string
          description: ISBN-10 or ISBN-13 (hyphens/spaces allowed)
      responses:
        "200":
          description: Book found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BookDetails"
        "400":
          description: Invalid ISBN
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: All successful catalog lookups had no book for this ISBN
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Catalog services failed and no fallback result was available
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /books/search:
    get:
      operationId: searchBooks
      tags: [books]
      summary: Search books by title and optional metadata
      description: Searches Google Books with a server-only key, retries without publisher when needed, and falls back to Open Library. Returns multiple deduplicated candidates.
      parameters:
        - name: title
          in: query
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 200
          description: Book title
        - name: author
          in: query
          schema:
            type: string
            maxLength: 200
          description: Optional author name
        - name: publisher
          in: query
          schema:
            type: string
            maxLength: 200
          description: Optional publisher name
      responses:
        "200":
          description: Matching book candidates
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BookDetails"
        "400":
          description: Missing or invalid search parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: All successful catalog searches had no matches
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Catalog services failed and no fallback result was available
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
components:
  schemas:
    IdentifyCoverRequest:
      type: object
      properties:
        imageBase64:
          type: string
          description: Base64-encoded image bytes (no data URL prefix)
        mimeType:
          type: string
          enum: [image/jpeg, image/png, image/webp]
      required:
        - imageBase64
        - mimeType
    CoverAnalysis:
      type: object
      properties:
        title:
          type: string
        authors:
          type: array
          items:
            type: string
        publisher:
          type: string
        editionText:
          type: string
        possibleIsbn:
          type: string
        confidence:
          type: number
          minimum: 0
          maximum: 1
      required:
        - title
        - authors
        - publisher
        - editionText
        - possibleIsbn
        - confidence
    BookDetails:
      type: object
      properties:
        isbn:
          type: string
        title:
          type: string
        authors:
          type: array
          items:
            type: string
        description:
          type: string
        pageCount:
          type: number
        coverUrl:
          type: string
        publisher:
          type: string
        publishedDate:
          type: string
        categories:
          type: array
          items:
            type: string
        language:
          type: string
      required:
        - isbn
        - title
        - authors
        - description
        - pageCount
        - coverUrl
        - publisher
        - publishedDate
        - categories
        - language
    ErrorResponse:
      type: object
      properties:
        message:
          type: string
      required:
        - message
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
